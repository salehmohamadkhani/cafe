# راهنمای کامل پر کردن صفحه "ساخت پترن" در فراز SMS

این راهنما دقیقاً به شما می‌گوید که در هر فیلد صفحه "ساخت پترن" چه اطلاعاتی وارد کنید.

---

## 📋 اطلاعات مورد نیاز برای صفحه پترن

### 1️⃣ عنوان پترن (Pattern Title) ⭐
**فیلد:** "عنوان پترن *"

**مقدار پیشنهادی:**
```
ورود به پنل مدیریت - OTP
```

یا:
```
کد یکبار مصرف ورود
```

**نکته:** این عنوان فقط برای شناسایی پترن در پنل شماست.

---

### 2️⃣ متن پترن (Pattern Text) ⭐⭐⭐
**فیلد:** "متن پترن"

**⚠️ مهم:** این مهم‌ترین بخش است!

#### گزینه 1: با نام مجموعه/برند
```
کد ورود شما به پنل مدیریت کافه مادلین: {otp}
این کد تا 5 دقیقه معتبر است.
```

#### گزینه 2: ساده‌تر
```
کافه مادلین
کد ورود شما: {otp}
این کد تا 5 دقیقه معتبر است.
```

#### گزینه 3: با لینک وب‌سایت (اگر شرایط را دارید)
```
کد ورود شما: {otp}
این کد تا 5 دقیقه معتبر است.
https://yourdomain.com
```

**⚠️ قوانین مهم:**
- ✅ **حتماً نام مجموعه/برند باید در متن باشد** (مثلاً "کافه مادلین")
- ✅ متغیر `{otp}` باید در متن باشد
- ✅ متن باید هدفمند باشد (برای ارسال رمز یکبار مصرف)
- ❌ نام مجموعه نمی‌تواند به عنوان متغیر استفاده شود
- ❌ لینک نمی‌تواند به متغیر تبدیل شود

---

### 3️⃣ ساخت متغیر (Create Variable) ⭐⭐
**دکمه:** "ساخت متغیر جدید" (+)

**مراحل:**
1. روی دکمه "ساخت متغیر جدید" کلیک کنید
2. در فیلد نام متغیر، دقیقاً این را وارد کنید:
   ```
   otp
   ```
3. نوع متغیر را **"رشته"** (String) انتخاب کنید
4. ذخیره کنید

**نکته:** نام متغیر باید دقیقاً `otp` باشد (با حروف کوچک).

---

### 4️⃣ متغیرهای استفاده شده (Used Variables)
بعد از ساخت متغیر، باید در این بخش نمایش داده شود:
```
otp (رشته)
```

اگر چیزی نمایش داده نشد، متغیر را دوباره بسازید.

---

### 5️⃣ توضیحات پترن (Pattern Description) ⭐
**فیلد:** "توضیحات پترن *"

**مقدار پیشنهادی:**
```
ارسال کد یکبار مصرف برای ورود به پنل مدیریت مرکزی سیستم کافه
```

یا:
```
کد تایید ورود به پنل مدیریت
```

**نکته:** این توضیحات برای بررسی پترن توسط فراز SMS استفاده می‌شود.

---

### 6️⃣ وب‌سایت شما (Your Website)
**فیلد:** "وب‌سایت شما"

**مقدار:**
- اگر وب‌سایت دارید: `https://yourdomain.com`
- اگر وب‌سایت ندارید: می‌توانید خالی بگذارید یا آدرس IP سرور را وارد کنید

**نکته:** این فیلد اختیاری است اما بهتر است پر شود.

---

## ✅ چک‌لیست قبل از ارسال

قبل از کلیک روی دکمه "تایید"، بررسی کنید:

- [ ] عنوان پترن وارد شده است
- [ ] متن پترن شامل نام مجموعه/برند است
- [ ] متغیر `{otp}` در متن پترن وجود دارد
- [ ] متغیر `otp` به عنوان "رشته" تعریف شده است
- [ ] توضیحات پترن وارد شده است
- [ ] متن پترن هدفمند است (برای ارسال رمز یکبار مصرف)

---

## 📤 بعد از ارسال

1. روی دکمه **"تایید"** کلیک کنید
2. منتظر تایید پترن توسط فراز SMS بمانید (معمولاً چند ساعت تا یک روز)
3. بعد از تایید، به بخش "پیامک‌های متغیردار (پترن)" بروید
4. **کد پترن** تایید شده را کپی کنید (مثلاً: `abc123def456`)
5. این کد را در تنظیمات Environment Variables استفاده کنید:
   ```powershell
   $env:SMS_PATTERN_CODE="کد_پترن_شما"
   ```

---

## 🔧 تنظیمات در کد

بعد از دریافت کد پترن، این متغیرها را تنظیم کنید:

```powershell
$env:SMS_PROVIDER="farazsms"
$env:SMS_USERNAME="username_شما"
$env:SMS_PASSWORD="password_شما"
$env:SMS_SENDER="شماره_فرستنده_شما"
$env:SMS_PATTERN_CODE="کد_پترن_تایید_شده"
```

---

## ❌ خطاهای رایج

### پترن رد شد - نام مجموعه در متن نیست
**راه حل:** حتماً نام مجموعه/برند خود را در متن پترن قرار دهید.

### پترن رد شد - متن هدفمند نیست
**راه حل:** در توضیحات و متن، واضح بنویسید که برای ارسال رمز یکبار مصرف است.

### متغیر تعریف نشده
**راه حل:** حتماً متغیر `otp` را به عنوان "رشته" تعریف کنید.

---

## 📞 پشتیبانی

اگر مشکلی داشتید:
1. لاگ‌های سرور را بررسی کنید
2. پاسخ API فراز SMS را بررسی کنید
3. مطمئن شوید که پترن تایید شده است

---

## 💡 مثال کامل

**عنوان پترن:**
```
ورود به پنل مدیریت - OTP
```

**متن پترن:**
```
کد ورود شما به پنل مدیریت کافه مادلین: {otp}
این کد تا 5 دقیقه معتبر است.
```

**متغیر:**
- نام: `otp`
- نوع: `رشته`

**توضیحات:**
```
ارسال کد یکبار مصرف برای ورود به پنل مدیریت مرکزی
```

**وب‌سایت:**
```
https://yourdomain.com
```

---

**موفق باشید! 🎉**


# معماری UI و انبارداری پلتفرم کافه

## تصویر کلی

پلتفرم یک کد مشترک دارد، یک دیتابیس مادر برای مدیریت کافه‌ها و یک دیتابیس عملیاتی مستقل برای هر کافه. پنل مادر مالک این تصمیم‌هاست:

- فعال یا غیرفعال بودن کافه
- ماژول‌های مجاز
- کاربر و نقش اولیه
- قرارداد انبارداری: بدون انبار، یک انبار مرکزی یا چند انبار مستقل
- ورود مدیریتی مستقیم (Master SSO) با ثبت رویداد audit

هر کافه داده‌های سفارش، مشتری، منو، خرید، مواد اولیه، موجودی و کاربران خودش را در دیتابیس مستقل نگه می‌دارد.

دکمه «ورود» فقط با سشن معتبر master، کاربر فعال مدیر کافه را انتخاب و سشن tenant را ایجاد می‌کند. رویداد `cafe.master_sso.entered` برای ممیزی ثبت می‌شود. لینک «بازگشت به مرکز فرمان» سشن tenant را پاک می‌کند اما سشن master را نگه می‌دارد.

### قرارداد دسترسی مؤثر

- نقش ذخیره‌شده کاربر کافه، هویت عملیاتی اوست و در ورود مرکزی تغییر نمی‌کند.
- `effective_role` در Master SSO برابر `superadmin` است؛ بنابراین محدودیت cashier، inventory و waiter روی ناظر مرکزی اعمال نمی‌شود.
- Master SSO همه ماژول‌ها را برای بازرسی می‌بیند، اما تنظیم `CafeModule` کافه را تغییر نمی‌دهد.
- کاربر عادی فقط منوها و routeهای ماژول‌های فعال خودش را می‌بیند.
- غیرفعال شدن حساب master، نشست نظارت tenant را فوراً باطل می‌کند.
- همه tenantها از route مشترک `/dashboard/` استفاده می‌کنند؛ routeهای قدیمی `/cafe/<slug>/dashboard` فقط compatibility redirect هستند.

## نقشه وابستگی انبار

```text
تعریف کافه در دیتابیس مادر
  ├── CafeModule(inventory)
  └── CafeWarehouseProfile + CafeWarehouseDefinition
          ↓ provisioning
InventoryConfiguration + Warehouse در دیتابیس کافه
          ↓
MaterialPurchase / RawMaterial
          ↓
WarehouseTransfer + WarehouseMaterialMinStock
          ↓
MenuItemMaterial و رسپی منو
          ↓
PreProductionStock / Production / Transfer
          ↓
مصرف مواد در OrderItem و گزارش بهای تمام‌شده
```

قاعده مهم: صفحه انبار دیگر اجازه ندارد یک لیست ثابت را روی کافه تحمیل کند. کافه‌های قدیمی فقط در صورت نداشتن قرارداد مادر با رفتار legacy پشتیبانی می‌شوند.

## ممیزی صفحه‌به‌صفحه

| صفحه | نقش | وابستگی انبار | وضعیت طراحی |
|---|---|---|---|
| پنل مدیریت مرکزی | ساخت و کنترل کافه‌ها | تعریف توپولوژی اولیه | بازطراحی کامل |
| داشبورد مدلین | میز، سفارش، فروش | اثر غیرمستقیم از مصرف سفارش | یکپارچه با Design System v2 |
| مواد اولیه و خرید | ثبت ماده و ورود خرید | ورودی زنجیره موجودی | یکپارچه با Design System v2 |
| مدیریت انبارها | انتقال و موجودی تفکیکی | هسته چندانباره | یکپارچه با Design System v2 |
| منو و قیمت‌گذاری | آیتم و رسپی | اتصال ماده/پیش‌تولید به آیتم منو | یکپارچه با Design System v2 |
| گزارش مالی | فروش و تسویه | اثر بهای مواد و خرید | یکپارچه با Design System v2 |
| کاربران و نقش‌ها | دسترسی عملیاتی | نقش inventory/procurement | یکپارچه با Design System v2 |
| تنظیمات کافه | مشخصات و هزینه‌ها | ورودی فرمول بهای تمام‌شده | یکپارچه با Design System v2 |

## Design System v2

مرجع نهایی `static/css/design-system-v2.css` است و بعد از استایل‌های قدیمی بارگذاری می‌شود. قواعد اصلی:

- رنگ اصلی سبز تیره با accent گرم، مناسب محصول کافه و خوانایی پنل مدیریتی
- ارتفاع کنترل استاندارد: ۴۲ پیکسل؛ موبایل: ۴۴ پیکسل
- شعاع‌ها: ۸، ۱۲، ۱۸ و ۲۴ پیکسل
- فاصله‌ها بر پایه مضرب ۴
- focus ring قابل‌دیدن و کنتراست مناسب
- جدول اسکرول‌پذیر، مودال پاسخ‌گو و دکمه‌های primary/secondary/ghost/danger
- سازگاری با کلاس‌های قبلی برای مهاجرت تدریجی بدون شکستن رفتار صفحات

## سناریوهای پشتیبانی‌شده

1. بدون انبار: ماژول inventory خاموش است یا توپولوژی `none` دارد.
2. مرکزی: خرید و مصرف در یک `Warehouse` انجام می‌شود.
3. چندانباره: اولین انبار، انبار اصلی است و انبارهای بعدی مقصد انتقال و مصرف هستند.

تغییر توپولوژی یک کافه دارای تراکنش باید در آینده با workflow مهاجرت و تأیید صریح انجام شود؛ حذف مستقیم انبار دارای موجودی نباید مجاز باشد.
